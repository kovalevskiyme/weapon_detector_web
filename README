#  Краткий гайд по использованию веб-сервиса

Веб-приложение для детекции опасных предметов на фото (ножи/пистолеты и др.) с использованием предобученной модели **OWL-ViT** (zero-shot).

## 📋 Основные функции
- Загрузка изображения и запуск детекции
- Визуализация bounding boxes на изображении
- История запросов (JSON)
- Экспорт истории в **Excel** и **PDF**
- Очистка истории

---

## 1️⃣ Установка окружения

### Необходимое ПО
- **Python 3.10+** (рекомендуется 3.12)
- **pip** (менеджер пакетов)
- **Git** (для клонирования репозитория)
- **VS Code** или любой другой редактор

### Проверка установки
```bash
python3 --version
pip3 --version
```

---

## 2️⃣ Получение проекта

### Вариант A: Скачать архив
1. Скачайте ZIP-архив с GitHub
2. Распакуйте в папку, например: `~/projects/weapon_detector_web`

### Вариант B: Клонировать репозиторий
```bash
git clone <ССЫЛКА_НА_РЕПОЗИТОРИЙ>
cd weapon_detector_web
```

---

## 3️⃣ Настройка проекта в VS Code
1. Откройте VS Code
2. **File → Open Folder…**
3. Выберите папку проекта `weapon_detector_web`

---

## 4️⃣ Установка зависимостей

### 4.1 Создание виртуального окружения
```bash
python3 -m venv .venv
```

### 4.2 Активация окружения
```bash
source .venv/bin/activate
```
*(После активации в терминале появится `(.venv)`)*

### 4.3 Обновление pip
```bash
pip install --upgrade pip
```

### 4.4 Установка пакетов
```bash
pip install fastapi uvicorn jinja2 python-multipart pillow torch transformers pandas openpyxl reportlab
```
*Примечание: установка `torch` может занять несколько минут.*

---

## 5️⃣ Структура проекта
```
weapon_detector_web/
├── app/
│   ├── main.py              # Сервер FastAPI
│   ├── templates/           # HTML-шаблоны
│   └── static/              # Статические файлы
├── services/
│   ├── inference.py         # Логика детекции
│   ├── history.py           # Работа с историей
│   └── report.py            # Экспорт отчётов
├── data/
│   ├── history.json         # Файл истории
│   └── reports/             # Папка для отчётов
└── README.md
```

---

## 6️⃣ Запуск сервера

### Команда запуска
```bash
uvicorn app.main:app --reload
```

### Доступные адреса
- Главная страница: [http://127.0.0.1:8000](http://127.0.0.1:8000)
- Страница истории: [http://127.0.0.1:8000/history](http://127.0.0.1:8000/history)

### Остановка сервера
Нажмите **Ctrl + C** в терминале.

---

## 7️⃣ Использование веб-интерфейса

### 7.1 Детекция объектов
1. **Загрузите изображение** (кнопка "Выберите файл")
2. **Введите метки** (labels) на английском через запятую
3. **Настройте порог уверенности** (threshold)
4. Нажмите **"Обработать"**

### 7.2 Примеры меток
| Объект | Рекомендуемые метки |
|--------|---------------------|
| Нож | `knife, kitchen knife, blade` |
| Пистолет | `pistol, semi-automatic pistol, gun` |
| Револьвер | `revolver, pistol, gun` |

*Примечание: используется техника **ensemble labels** — система автоматически расширяет список синонимами.*

### 7.3 Настройка порога (Threshold)
- **Низкий порог (0.08-0.12)**: больше обнаружений, но возможны ложные срабатывания
- **Высокий порог (0.20-0.30)**: меньше ложных срабатываний, но можно пропустить объекты

*Рекомендуется начинать с **0.15***

---

## 8️⃣ Работа с историей и отчётами

### 8.1 Просмотр истории
Перейдите на страницу [http://127.0.0.1:8000/history](http://127.0.0.1:8000/history)
- Просмотр всех запросов
- Превью результатов
- Кнопка **"Очистить историю"**

### 8.2 Экспорт в Excel
Кнопка **"Экспорт в Excel"** на странице истории или прямой доступ:
```
http://127.0.0.1:8000/export/excel
```
Файл сохраняется в: `data/reports/history.xlsx`

### 8.3 Экспорт в PDF
Кнопка **"Экспорт в PDF"** или прямой доступ:
```
http://127.0.0.1:8000/export/pdf
```
Файл сохраняется в: `data/reports/history.pdf`

---

## 9️⃣ Решение частых проблем

### 9.1 Ошибки зависимостей
Убедитесь, что виртуальное окружение активировано:
```bash
source .venv/bin/activate
```

### 9.2 "ModuleNotFoundError"
Установите отсутствующий пакет:
```bash
pip install <имя_пакета>
```

### 9.3 Модель не находит объекты
1. **Используйте другие формулировки** (например `kitchen knife` вместо `knife`)
2. **Уменьшите порог** до `0.08-0.12`
3. **Используйте качественные изображения** с чёткими объектами

**Удачи в использовании!** 
